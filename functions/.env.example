# Firebase Functions environment variables (optional scaffolding)
# Copy to functions/.env.local when needed by callable/background functions.

# Example:
# EXTERNAL_API_BASE_URL=https://api.example.com
